import { Schema } from '../../declarations';
import { SetupContext } from 'vue';

export declare const renderSchema: (schemas: Array<Schema>, props: Record<string, unknown>, context: SetupContext) => any;
